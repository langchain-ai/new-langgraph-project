# ============================================================================
# LangSmith Configuration (Optional - for tracing)
# ============================================================================
LANGSMITH_PROJECT=lucart-agents-api
# LANGSMITH_API_KEY=your-langsmith-api-key

# ============================================================================
# Anthropic Claude Configuration (Required)
# ============================================================================
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Optional Claude model configuration
# CLAUDE_MODEL=claude-sonnet-4-5-20250929
# CLAUDE_TEMPERATURE=0.0
# CLAUDE_MAX_TOKENS=4096

# ============================================================================
# Database Configuration
# ============================================================================

# Database Mode Selection
# Set to "true" to use remote Azure PostgreSQL, "false" for local PostgreSQL
USE_REMOTE_DB=false

# ----------------------------------------------------------------------------
# Remote Database Configuration (Azure PostgreSQL)
# Required when USE_REMOTE_DB=true
# ----------------------------------------------------------------------------
# Complete connection string provided by Azure Portal
# Format: postgresql://username:password@host:port/database?sslmode=require
# Example: postgresql://admin:pass123@pg-lucart-dev.postgres.database.azure.com:5432/lucart_db_dev?sslmode=require
DB_URL=

# ----------------------------------------------------------------------------
# Local Database Configuration (Local PostgreSQL)
# Required when USE_REMOTE_DB=false
# ----------------------------------------------------------------------------
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=lucart_local
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# ----------------------------------------------------------------------------
# Database Schema Configuration (Both modes)
# ----------------------------------------------------------------------------
# Schema name containing audit tables (default: acr)
POSTGRES_SCHEMA=acr
